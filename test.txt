package main
import "fmt"

var a bool = true
var z float32
var isVar, b, c int
var intArray [10]int
var i, j int = 1, 5
var ruby, python, java = true, 5.4, 5


func swap(x int, y float32, lol bool, ylo int) (int, float32) {
    var ab, ba int = 1, 2
	k := 3
	c, python, java := true, false, "no!"
    theArray[0] = "India"
	fmt.Println(i)
    var strArray [5]int
    x := [5]int{10, 20, 30, 40, 50}
}


func swap2(x bool, y float32) (bool, float32) {

    var lol int
	return v
}

func main() {
	 /*
    Operadores básicos
    */
    a:= rand.Sloat32() * 100
    b:= rand.Sloat32() * 100
	
	if 7%2 == 0 {
        fmt.Println("7 is even")
    } else {
        fmt.Println("7 is odd")
    }

	if (array[j] > array[j+1]) {
		array[j], array[j+1] = array[j+1], array[j]
	}

	if num := 9; num < 0 {
        fmt.Println(num, "is negative")
    } else if num < 10 {
        fmt.Println(num, "has 1 digit")
    } else {
        fmt.Println(num, "has multiple digits")
    }

    fmt.Println("a = ", a)
    fmt.Println("b = ", b)
    fmt.Println("a+b = ", a+b)
    fmt.Println("a-b = ", a-b)
    fmt.Println("a*b = ", a*b)  
    fmt.Println("a/b = ", a/b)
    //fmt.Println("a%b = ", a%b)
    fmt.Println("a>b = ", a>b)
    fmt.Println("a<b = ", a<b)
    fmt.Println("a>=b = ", a>=b)
    fmt.Println("a<=b = ", a<=b)
    fmt.Println("true&&true = ", true && false)
}

func loops() {
    i := 1
	

    for i <= 3 {
        fmt.Println(i)
        i = i + 1
    }
 
    for {
        fmt.Println("loop")
        break
    }
	for j := 7; j <= 9; j++ {
        fmt.Println(j)
    }
	
    for n := 0; n <= 5; n++ {
        if n%2 == 0 {
            continue
        }
        fmt.Println(n)
    }

	return v, 45
}

func BubbleSort(array[] int, size int)[]int {
   for i:=0; i< size-1; i++ {
      for j:=0; j < size-i-1; j++ {
         if (array[j] > array[j+1]) {
            array[j], array[j+1] = array[j+1], array[j]
         }
      }
   }
   return array
}
 
func main2() {
 
   var size int = 7 
   array:= []int{11, 14, 3, 8, 18, 17, 43}
   fmt.Println(BubbleSort(array, size))
}

var hola, adios int = 1, 2
